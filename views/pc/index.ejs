<!DOCTYPE html>
<html lang="en">
<head>
  <% include partials/header %>
</head>
<body>
  <div>for PC</div>

  <% offers.forEach(function(offer) { %>
      <div><%= offer.name %> - <%= offer.description %></div>
  <% }) %>

  <!-- script -->
  <% include partials/scripts %>
</body>
</html>